import { Checkbox as BaseCheckbox } from "semantic-ui-react";
import React from "react";

const Checkbox = props => {
  return <BaseCheckbox {...props} className="idseq-ui-checkbox" />;
};

export default Checkbox;
